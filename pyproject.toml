[project]
name = "clinotes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mypy>=2.1.0",
    "prompt-toolkit>=3.0.52",
]

[dependency-groups]
dev = [
    "mypy>=2.1.0",
]
